Variables: Las Variables son espacios en donde podemos guardar información para luego volver a utilizar, son como cajas en donde almacenamos datos,valores (cualquier tipo de información) que etiquetamos con un nombre con el que vamos a poder encontrarlo luego.
Strings: Las Strings son un tipo de dato nativo de JavaScript, son bloques de texto o también podemos explicarlos como una cadena de caracteres esto quiere decir que puede estar conformado por signos, letras, números y hasta espacios y tienen como característica que siempre van entre comillas que pueden ser dobles o simples. 
Booleans: Los Boolean son otro tipo de dato de JavaScript y  provienen de la lógica booleana y lo que establecen es que existen dos posibles resultados de valor de verdad: true o false. Con esos varoles es que se trabaja para establecer si una expresion o la comparación de dos expresiones es verdadera o falsa en todo caso.
Undefined: Undefine es un resultado que es utilizado por defecto en JavaScript y lo que nos quiere expresar es que no hay un valor asociado o simplemente que lo que estamos buscando no existe. 
Null: Null quiere decir NADA y a diferencia de undefine es introducido por el propio desarrollador, este quiere expresar que en ese espacio no existe un valor, o que no está definido. Es solo incorporado por el propio editor.
Operadores: Los operadores son los simbolos matemáticos que utilizamos para idicarle al sistema que debe realizar una operación matemática, estos son (+,-,*,/)
== vs. ===: Es importante tener presente la diferencia entre estos dos operadores de compración; el doble igual comparará dos valores para saber si son iguales o no y triple igual también realizará una compración entre dos valores pero este a diferencia tomara en cuenta para su análisis el tipo de dato de cada uno.(Ejemplo: 24 == '24' == true ; 24 === '24' === false).Es por eso que se considera una buena práctica siempre el uso del triple igual para no caer el posibles errores. 